name: Set up .NET environment
runs:
    using: "composite"
    steps:
        -   name: Set up .NET Core SDK
            uses: actions/setup-dotnet@v5
            with:
                dotnet-version: |
                    8.x
                    9.x
                    10.x
